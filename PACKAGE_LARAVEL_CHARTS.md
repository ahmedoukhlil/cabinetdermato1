# Package Laravel Charts installé

## ✅ Installation réussie

Le package `laraveldaily/laravel-charts` version **0.2.3** a été installé avec succès.

## 📦 Détails

- **Package** : `laraveldaily/laravel-charts`
- **Version** : 0.2.3
- **Namespace** : `LaravelDaily\LaravelCharts\Classes\LaravelChart`
- **Fichier** : `vendor/laraveldaily/laravel-charts/src/Classes/LaravelChart.php`

## ✅ Utilisation

Le package est utilisé dans `app/Http/Controllers/Admin/HomeController.php` :

```php
use LaravelDaily\LaravelCharts\Classes\LaravelChart;

$chart1 = new LaravelChart($settings1);
$chart2 = new LaravelChart($settings2);
```

## 📝 Note

Le package a été ajouté à `composer.json` avec la contrainte `^0.1.14`, mais Composer a installé la version `0.2.3` qui est compatible.

